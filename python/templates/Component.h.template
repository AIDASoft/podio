#ifndef ${PACKAGE_NAME}_${name}_H
#define ${PACKAGE_NAME}_${name}_H
$includes

#include <iostream>

${namespace_open}
class $name {
public:
$members
$extracode_declarations
};

${ostreamComponents}

${namespace_close}

#endif
