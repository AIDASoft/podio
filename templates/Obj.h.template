#ifndef ${name}OBJ_H
#define ${name}OBJ_H

// std includes
#include <atomic>
#include <iostream>

// data model specific includes
#include "albers/ObjBase.h"
#include "${name}Data.h"

$includes

// forward declarations
class ${name};

class ${name}Obj : public albers::ObjBase {
public:
  ${name}Obj();
  ${name}Obj(const ${name}Obj&); //TODO: deep copy!
  ${name}Obj(const albers::ObjectID id, ${name}Data data);
  virtual ~${name}Obj();

public:
  ${name}Data data;
${relations}

};


#endif
