options:
  includeSubfolder: true

schema_version: 1

components:
  edm4hepLike::Vector2i:
    Description: "A component that will be removed entirely"
    Members:
      - int a
      - int b

  edm4hepLike::Vector3d:
    Members:
      - double x
      - double y
      - double z

datatypes:
  ExampleMC:
    Description: "An example for a class where an older version had a component that is entirely dropped"
    Author: "Thomas Madlener"
    Members:
      - int PDG // pdg
      - double energy // energy
      - edm4hepLike::Vector2i colorFlow // component that will be dropped entirely
      - edm4hepLike::Vector3d momentum // momentum
